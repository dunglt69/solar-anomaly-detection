{
  "name": "energiamind-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise Solar PV Telemetry Monitoring & AI Anomaly Detection Platform",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "bootstrap": "npm install",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build:server": "npm run build --workspace=server",
    "build:client": "npm run build --workspace=client",
    "test:server": "npm run test --workspace=server"
  },
  "dependencies": {
    "docx": "^9.7.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  },
  "overrides": {
    "onnxruntime-node": {
      "adm-zip": "^0.6.0"
    },
    "drizzle-kit": {
      "esbuild": "^0.25.0"
    },
    "@esbuild-kit/core-utils": {
      "esbuild": "^0.25.0"
    },
    "adm-zip": "^0.6.0",
    "esbuild": "^0.25.0"
  }
}
